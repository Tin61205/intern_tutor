[pytest]
asyncio_mode = auto
pythonpath = .
markers =
    integration: Test tích hợp - cần Database hoặc Redis thật (dùng client/db_session fixture)
    unit: Test đơn vị thuần túy - không cần DB hay Redis